<nav class="navbar navbar-expand-lg custom-navbar">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand text-white" do %>
            <i class="bi bi-calendar-check me-2"></i>
            Trebol
    <% end %>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link active text-white" aria-current="page" href="<%= boards_path %>">Boards</a>
        <a class="btn btn-primary custom-button text-white" 
           href="<%= new_board_path %>"
           style="--bs-btn-padding-y: .25rem; --bs-btn-padding-x: .5rem; --bs-btn-font-size: .75rem;">
           Create
        </a>
      </div>

    <div class="container-fluid">
      <div class="container d-flex justify-content-end align-items-center">
        <% if user_signed_in? %>
          <%= link_to user_path(current_user), class: "d-flex align-items-center me-3" do %>
              <i class="bi bi-person-circle icon-color fs-1"></i>
          <% end %>
          <%= link_to "Logout", destroy_user_session_path, class: "btn btn-primary custom-button-3 text-white ms-2", data: { turbo_method: :delete } %>
        <% else %>
          <%= link_to 'Login', new_user_session_path, class: "btn btn-primary custom-button-3 text-white ms-2" %>
          <%= link_to 'Sign up', new_user_registration_path, class: "btn btn-primary custom-button text-white ms-2" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>